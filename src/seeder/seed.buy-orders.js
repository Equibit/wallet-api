const { faker, objectid, randomFloat, randomInt } = require('./utils')

module.exports = function seedBuyOrders (app, issuances) {
  const seederService = app.service('seeder')
  const buyOrders = {}

  for (var i = 0; i < 100; i++) {

  }


  return Promise.resolve(buyOrders)
}
